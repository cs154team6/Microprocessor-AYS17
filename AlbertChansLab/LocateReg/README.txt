1) Suffix _left/_right means you expect the Register is on the _left/_right
   As of now, I did not implement a way to go back and track the other way if the read head is going too far.
2) After register is located, the read head stays on the Register character, NOT register's content.